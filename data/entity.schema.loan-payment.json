{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/entity.schema.loan-payment.json",
  "title": "LoanPayment Entity Schema",
  "description": "JSON schema for LoanPayment entity in the event-based processing system",
  "type": "object",
  "properties": {
    "entity": {
      "$ref": "entity.schema.header.json"
    },
    "id": {
      "type": "string",
      "description": "Unique identifier for the payment"
    },
    "loanId": {
      "type": "string",
      "description": "Identifier of the loan this payment belongs to"
    },
    "amount": {
      "type": "number",
      "minimum": 0,
      "multipleOf": 0.01,
      "description": "Payment amount"
    },
    "paymentDate": {
      "type": "string",
      "format": "date-time",
      "description": "Date and time when the payment was made"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the payment record was created"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the payment record was last updated"
    }
  },
  "required": ["entity", "id", "loanId", "amount", "paymentDate", "createdAt", "updatedAt"],
  "additionalProperties": false
}
