# Flink Application Configuration
# Bootstrap servers for MSK cluster
# This is a fallback - the application will try to read from:
# 1. System property: bootstrap.servers
# 2. Environment variable: BOOTSTRAP_SERVERS
# 3. Command line argument
# 4. TableEnvironment configuration (from property groups)
# 5. This properties file (fallback)

# Default bootstrap servers
# This is a fallback value - the application will try multiple sources
bootstrap.servers=boot-yjc0v78h.c1.kafka-serverless.us-east-1.amazonaws.com:9098

