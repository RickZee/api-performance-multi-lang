{
  "_comment": "✅ RECOMMENDED: This config includes ExtractNewRecordState transform to add __op, __table, __ts_ms fields required by Flink statements. See RECOMMENDED_APPROACH.md for details. Streams from event_headers table.",
  "name": "postgres-cdc-source-event-headers",
  "config": {
    "connector.class": "PostgresCdcSource",
    "name": "postgres-cdc-source-event-headers",
    "kafka.api.key": "${KAFKA_API_KEY}",
    "kafka.api.secret": "${KAFKA_API_SECRET}",
    "output.data.format": "JSON",
    "database.hostname": "${DB_HOSTNAME}",
    "database.port": "${DB_PORT:-5432}",
    "database.user": "${DB_USERNAME}",
    "database.password": "${DB_PASSWORD}",
    "database.dbname": "${DB_NAME}",
    "database.server.name": "${DB_SERVER_NAME:-aurora-postgres-cdc}",
    "database.sslmode": "${DB_SSLMODE:-require}",
    "table.include.list": "${TABLE_INCLUDE_LIST:-public.event_headers}",
    "plugin.name": "pgoutput",
    "slot.name": "${SLOT_NAME:-event_headers_cdc_slot}",
    "snapshot.mode": "initial",
    "output.data.key.format": "JSON",
    "output.data.value.format": "JSON",
    "tasks.max": "1",
    "transforms": "unwrap,route",
    "transforms.unwrap.type": "io.debezium.transforms.ExtractNewRecordState",
    "transforms.unwrap.drop.tombstones": "false",
    "transforms.unwrap.add.fields": "op,table,ts_ms",
    "transforms.unwrap.add.fields.prefix": "__",
    "transforms.route.type": "io.confluent.connect.cloud.transforms.TopicRegexRouter",
    "transforms.route.regex": "${TOPIC_PREFIX:-aurora-postgres-cdc}\\.public\\.event_headers",
    "transforms.route.replacement": "raw-event-headers"
  }
}
