AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Producer API Python REST - Lambda Serverless Deployment with Aurora DSQL

Parameters:
  DatabaseURL:
    Type: String
    Description: PostgreSQL database connection string
    Default: postgresql://postgres:password@localhost:5432/car_entities
  AuroraEndpoint:
    Type: String
    Description: Aurora endpoint (optional, used if DATABASE_URL not provided)
    Default: ""
  DatabaseName:
    Type: String
    Description: Database name (optional, used with AuroraEndpoint)
    Default: car_entities
  DatabaseUser:
    Type: String
    Description: Database user (optional, used with AuroraEndpoint)
    Default: postgres
  DatabasePassword:
    Type: String
    Description: Database password (optional, used with AuroraEndpoint)
    NoEcho: true
    Default: ""
  LogLevel:
    Type: String
    Description: Logging level
    Default: info
    AllowedValues:
      - debug
      - info
      - warn
      - error
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC ID for Aurora access (optional)
    Default: ""
  SubnetIds:
    Type: CommaDelimitedList
    Description: Subnet IDs for Aurora access (optional)
    Default: ""
  AuroraDSQLEndpoint:
    Type: String
    Description: Aurora DSQL cluster endpoint (for IAM authentication)
    Default: ""
  AuroraDSQLPort:
    Type: Number
    Description: Aurora DSQL port (default 5432)
    Default: 5432
  IAMDatabaseUser:
    Type: String
    Description: IAM database username for Aurora DSQL
    Default: ""
  AuroraDSQLClusterResourceId:
    Type: String
    Description: Aurora DSQL cluster resource ID for IAM permissions (format: cluster-xxxxx)
    Default: ""

Globals:
  Function:
    Timeout: 30
    MemorySize: 512
    Runtime: python3.11
    Environment:
      Variables:
        LOG_LEVEL: !Ref LogLevel

Resources:
  ProducerApiPythonRestFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: producer-api-python-rest-lambda-dsql
      CodeUri: build/package/
      Handler: lambda_handler.handler
      Runtime: python3.11
      Architectures:
        - x86_64
      MemorySize: 512
      Timeout: 30
      Environment:
        Variables:
          DATABASE_URL: !If
            - HasDatabaseURL
            - !Ref DatabaseURL
            - !Sub "postgresql://${DatabaseUser}:${DatabasePassword}@${AuroraEndpoint}:5432/${DatabaseName}"
          AURORA_ENDPOINT: !Ref AuroraEndpoint
          DATABASE_NAME: !If
            - HasAuroraDSQL
            - !Ref DatabaseName
            - !Ref DatabaseName
          DATABASE_USER: !Ref DatabaseUser
          DATABASE_PASSWORD: !Ref DatabasePassword
          AURORA_DSQL_ENDPOINT: !Ref AuroraDSQLEndpoint
          AURORA_DSQL_PORT: !Ref AuroraDSQLPort
          IAM_USERNAME: !Ref IAMDatabaseUser
          AWS_REGION: !Ref AWS::Region
          LOG_LEVEL: !Ref LogLevel
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource: '*'
            - !If
              - HasAuroraDSQL
              - Effect: Allow
                Action:
                  - rds-db:connect
                Resource: !Sub "arn:aws:rds-db:${AWS::Region}:${AWS::AccountId}:dbuser:${AuroraDSQLClusterResourceId}/${IAMDatabaseUser}"
              - !Ref AWS::NoValue
      VpcConfig: !If
        - HasVpcConfig
        - SecurityGroupIds:
            - !Ref LambdaSecurityGroup
          SubnetIds: !Ref SubnetIds
        - !Ref AWS::NoValue
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref ProducerApiHttpApi
            Path: /{proxy+}
            Method: ANY
            PayloadFormatVersion: "2.0"

  ProducerApiHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      Name: producer-api-python-rest-http-api
      Description: HTTP API for Producer API Python REST Lambda
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - GET
          - POST
          - OPTIONS
        AllowHeaders:
          - Content-Type
          - Authorization
        MaxAge: 300

  LambdaSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Condition: HasVpcConfig
    Properties:
      GroupDescription: Security group for Lambda function to access Aurora
      VpcId: !Ref VpcId
      SecurityGroupEgress:
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          CidrIp: 0.0.0.0/0
          Description: Allow outbound to Aurora PostgreSQL

Conditions:
  HasDatabaseURL: !Not [!Equals [!Ref DatabaseURL, ""]]
  HasVpcConfig: !And
    - !Not [!Equals [!Ref VpcId, ""]]
    - !Not [!Equals [!Join ["", !Ref SubnetIds], ""]]
  HasAuroraDSQL: !And
    - !Not [!Equals [!Ref AuroraDSQLEndpoint, ""]]
    - !Not [!Equals [!Ref IAMDatabaseUser, ""]]
    - !Not [!Equals [!Ref AuroraDSQLClusterResourceId, ""]]

Outputs:
  ApiUrl:
    Description: API Gateway HTTP API endpoint URL
    Value: !Sub "https://${ProducerApiHttpApi}.execute-api.${AWS::Region}.amazonaws.com"
    Export:
      Name: !Sub "${AWS::StackName}-ApiUrl"
  
  FunctionName:
    Description: Lambda function name
    Value: !Ref ProducerApiPythonRestFunction
    Export:
      Name: !Sub "${AWS::StackName}-FunctionName"
  
  FunctionArn:
    Description: Lambda function ARN
    Value: !GetAtt ProducerApiPythonRestFunction.Arn
    Export:
      Name: !Sub "${AWS::StackName}-FunctionArn"
