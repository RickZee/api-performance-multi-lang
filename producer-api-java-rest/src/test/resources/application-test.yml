spring:
  application:
    name: producer-api-test
  
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/car_entities
    username: postgres
    password: password
    pool:
      initial-size: 2
      max-size: 5
      max-idle-time: 30m
      max-acquire-time: 30s
      max-create-connection-time: 30s
  
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

server:
  port: 0

logging:
  level:
    com.example: DEBUG
    org.springframework.r2dbc: DEBUG
